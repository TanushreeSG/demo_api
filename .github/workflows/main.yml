
name: Main Workflow

on:
  workflow_run:
    workflows: ["Secondary Workflow"]  # Trigger this workflow only if "Secondary Workflow" is triggered
    types:
      - completed

jobs:
  main-job:
    runs-on: ubuntu-latest
    needs: secondary-job
     steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: Katalon Studio Github Action
      uses: katalon-studio/katalon-studio-github-action@v2
      with:
          version: '8.6.6'
          projectPath: '${{ github.workspace }}'
          args: '-noSplash -retry=0 -statusDelay=15 -testSuiteCollections="Test Suites\chain" -apiKey="${{secrets.KATALON_API_KEY}} --config -webui.autoUpdateDrivers=true'
     
